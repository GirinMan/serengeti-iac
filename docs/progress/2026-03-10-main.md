# 2026-03-10 Main Progress

## Session Notes

- 작업 대상: 현재 호스트를 `AGENTS.md` 기준 홈랩 서버 상태에 최대한 가깝게 올린다.
- 사용자 승인:
  - 500GB SSD를 `/mnt/primary` 로 마이그레이션 가능
  - 8TB HDD 2개는 ZFS mirror 초기화 가능
  - 단, `/home/girinman/Downloads/onedrive` 는 절대 손대지 않음

## Completed So Far

- 호스트 인벤토리 수집 스크립트 추가 및 실행
- bind mount 경로 변수화
- `make preflight`, `make storage-map` 추가
- 현재 호스트 감사 문서 및 운영자 메모 작성
- 현재 `.env` 에 비민감 실측값 일부 반영
- `/mnt/primary` 마이그레이션, `archive` ZFS mirror 구성, Layer 0 패키지/보안 기초 적용

## Current Phase

- Layer 3 애플리케이션 재설계 및 안정화
- 블로그를 `WordPress` 로, 작업 관리를 `Plane` 으로 추가
- `Directus` 는 기본 앱이 아니라 선택형 콘텐츠 API 후보로 낮춤
- backup cron 런타임 재기동 경로 정리

## Timeline

- 20:53
  - `make storage-map` 실행
  - 현재 호스트 디스크 by-id 후보와 마운트 상태를 raw 로그로 저장
- 20:56
  - 500GB SSD를 `/mnt/primary` 로 실제 재마운트
  - `/etc/fstab` 의 500GB SSD 엔트리를 `/mnt/primary` 기준으로 수정
- 20:58
  - `/data` bind mount는 사용자 요청에 따라 제거
  - `system/03_mount_primary.sh` 재실행으로 디렉토리 구조 생성 완료
- 20:59
  - `system/01_setup.sh` 실행 시작
  - 현재 `apt-get full-upgrade` 진행 중
- 21:00
  - 로컬 `.env` 에 비밀값과 기본 포트값을 생성해 채움
  - Cloudflare 토큰과 실제 도메인 값은 사람 입력 영역으로 유지
- 21:01
  - `openssh-server` 활성 상태 확인
  - SSH 리스너를 `2222/tcp` 단일 포트로 정리
  - 관리자 키 파일을 `/home/girinman/.ssh/serengeti_ed25519` 로 정리
- 21:02
  - UFW 활성화
  - `LOCAL_GW` 전체 허용, `LOCAL_SUBNET -> 2222/tcp` 허용, loopback 허용 적용
- 21:02
  - ZFS mirror `archive` 풀 온라인 상태 확인
  - `/mnt/archive` 마운트 확인
- 21:03
  - `cloudflared` 패키지 설치 완료
  - 토큰 없는 상태라 서비스 등록은 보류
- 21:03 이후
  - Docker 네트워크 생성 완료
  - Layer 2 데이터 스택 이미지 pull 및 순차 기동 진행
- 21:07
  - PostgreSQL, Neo4j, Elasticsearch, Redis, Kafka, Minio 모두 healthy 상태 확인
  - Nextcloud용 PostgreSQL DB `nextcloud` 생성 완료
- 21:11
  - 공식 `apache/kafka` 이미지로 Kafka 컨테이너 정상 기동
  - `redis` healthcheck 수정 후 healthy 전환
  - `nextcloud`, `backup-pipeline` 기동 시작
- 21:16
  - `backup-pipeline` 는 `setpgid: Operation not permitted` 로 재시작함을 확인
- 21:18
  - `backup` 이미지를 `busybox crond` 기준으로 단순화하도록 IaC 수정
  - post-install runbook에 backup 재빌드 절차를 반영
- 21:19
  - 현재 세션에서 Docker API 접근이 불안정해 실제 재빌드/재기동 검증은 운영 메모로 남김
- 21:20
  - Perplexity 검색으로 self-hosted CMS 후보를 비교
  - 기존 인프라 PostgreSQL을 재사용하고 콘텐츠 API를 즉시 제공하는 방향으로 1차적으로 `Directus` 를 선택
- 21:22
  - `docker/layer3-apps/content/docker-compose.yml`, `.env(.example)`, `Makefile` 을 Directus 기준으로 갱신
  - `docker compose ... config` 와 `make validate` 정적 검증 통과
  - 이후 사용자 피드백에 따라 `Directus` 는 기본 블로그 경로에서 제외하기로 결정
- 21:31
  - Perplexity 재검색으로 개발/취미 블로그에 더 적합한 플랫폼을 비교
  - 최종 블로그 플랫폼을 `WordPress` 로 확정
  - `WriteFreely` 는 미니멀 대안으로만 메모
- 21:36
  - Plane 공식 배포 구성을 기준으로 기존 인프라 재사용 가능 범위를 점검
  - 기존 `PostgreSQL`, `Redis`, `Minio` 를 재사용하고, Plane 요구사항 때문에 `RabbitMQ` 만 인프라 레이어에 추가하기로 확정
- 21:40
  - `docker/layer2-data/mariadb/docker-compose.yml` 추가
  - `docker/layer2-data/rabbitmq/docker-compose.yml` 추가
  - `docker/layer3-apps/blog/docker-compose.yml` 을 `WordPress` 기준으로 추가
  - `docker/layer3-apps/plane/docker-compose.yml` 추가
  - `.env.example`, `Makefile` 을 새 앱 구조 기준으로 갱신
- 21:44
  - `docker exec -i postgres ...` 로 `plane` PostgreSQL DB 생성 완료
  - `make validate` 재실행 통과
- 21:46
  - `mariadb`, `rabbitmq` 실제 기동 완료
  - 두 컨테이너 모두 `healthy` 상태 확인
- 21:48
  - `wordpress-blog` 이미지 pull 및 기동 완료
  - 현재 `healthy` 상태로 유지 중
- 21:49 이후
  - Plane 이미지 pull 도중 Docker Hub IPv6 경로에서 `network is unreachable` 오류 발생
  - 일부 Plane 이미지는 캐시에 정상 적재됐고, 남은 이미지를 개별 pull로 회복 중
  - 이미지 개별 pull 재시도로 전체 캐시 적재 완료
- 21:45
  - `docker compose ... plane up -d` 재실행
  - `plane-live` 의 Redis 인증 누락과 `plane-proxy` 의 빈 ACME 변수 문제를 식별
- 21:46
  - `docker/layer3-apps/plane/docker-compose.yml` 에 Redis 인증 URL, internal alias, proxy/data-tier 연결을 반영
  - `docker/layer2-data/minio/docker-compose.yml` 에 `plane-minio` alias 추가
- 21:47
  - `minio` 를 고정 태그 `RELEASE.2025-09-07T16-13-09Z` 로 재기동
  - 컨테이너 내부 `mc` 로 `plane-uploads` 버킷 생성 완료
- 21:48
  - `plane-proxy` 내부 HTTP 응답 확인
  - `plane-space` 기본 healthcheck 오탐을 Node 기반 체크로 교체
- 21:49
  - `plane-proxy`, `plane-admin`, `plane-web`, `plane-space` healthy 확인
  - `plane-migrator` 는 성공 종료 상태 확인

## Runtime Snapshot

- Healthy:
  - `postgres`, `neo4j`, `elasticsearch`, `redis`, `kafka`, `minio`
  - `mariadb`, `rabbitmq`
  - `nextcloud`, `wordpress-blog`, `npm`
  - `plane-proxy`, `plane-admin`, `plane-web`, `plane-space`
- Running:
  - `plane-api`, `plane-worker`, `plane-beat-worker`, `plane-live`
- Completed:
  - `plane-migrator`
- Deferred by human input:
  - Cloudflare Tunnel 토큰 등록
  - 실제 공인 도메인/DNS 연결

## Guardrails

- `/home/girinman/Downloads/onedrive` 는 보호 대상이며 이 세션에서 건드리지 않았다.
- 승인되지 않은 서비스 전용 DB 엔진 추가는 피했다.
- 다만 `WordPress` 는 표준 구성상 `MariaDB`, `Plane` 은 표준 구성상 `RabbitMQ` 가 필요해 인프라 레이어에 명시적으로 추가했다.
